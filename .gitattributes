# 윈도우 배치 파일은 반드시 CRLF 로 유지 (LF 면 cmd 에서 오동작)
*.bat text eol=crlf

# 맥 실행 스크립트는 반드시 LF (CRLF 면 bash 가 거부)
*.command text eol=lf
*.sh text eol=lf
**/Contents/MacOS/run text eol=lf

# 폰트는 바이너리
*.woff2 binary
